import os

# Pulls the hidden brain from the encrypted GitHub Vault
phantom_brain = os.environ.get("PHANTOM_VPROP_CODE")

# Executes the code entirely in the server's invisible memory
exec(phantom_brain)
